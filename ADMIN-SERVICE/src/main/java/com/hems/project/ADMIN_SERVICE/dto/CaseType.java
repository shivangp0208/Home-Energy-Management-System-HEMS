package com.hems.project.ADMIN_SERVICE.dto;

    public enum CaseType {
        SITE_OFFLINE,
        COMMAND_FAILED,
        LOW_SOC,
        VOLTAGE_ALERT,
        ENROLLMENT_CONFLICT,
        MANUAL
    }
