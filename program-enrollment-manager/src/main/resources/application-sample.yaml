spring:
  application:
    name: program-enrollment-manager

  datasource:
    url: jdbc:postgresql://localhost:5432/program_enrollment
    username:
    password:
    #    url: jdbc:postgresql://db.tootxwuhmwxfeotmnvus.supabase.co:5432/postgres?user=postgres&password=Hems%23Einfochips
    driver-class-name: org.postgresql.Driver

  cloud:
    config:
      import-check:
        enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: Hems#Einfochips

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8091/eureka



server:
  port: 8089

logging:
  level:
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: TRACE